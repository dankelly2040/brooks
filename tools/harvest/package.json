{
  "name": "@brooks/harvest",
  "version": "1.0.0",
  "private": true,
  "description": "Captures the real Brooks catalog into packages/catalog/catalog.json via a real browser session.",
  "scripts": {
    "harvest": "node harvest.js && node validate-editorial.js",
    "sync": "node sync.js",
    "validate:editorial": "node validate-editorial.js",
    "sniff": "node sniff-network.js",
    "probe:cart": "node probe-cart.js"
  },
  "dependencies": {
    "playwright": "^1.61.1"
  }
}
